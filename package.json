{
  "name": "policy-fund-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Smart search platform for Korean government policy funds using Gemini API",
  "scripts": {
    "start": "serve .",
    "build": "echo 'No build step required for browser-based Babel transform'"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}